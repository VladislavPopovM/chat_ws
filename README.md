# Minechat Listener

Асинхронный слушатель чата minechat с сохранением истории сообщений в файл.

## Возможности

- 🔄 Автоматическое переподключение при потере соединения
- 📝 Сохранение истории сообщений с временными метками
- 📺 Дублирование вывода в консоль
- ⚙️ Настройка через CLI аргументы и переменные окружения
- 🚀 Асинхронная работа с файлами (aiofiles)
- 🛡️ Корректное завершение по Ctrl+C

## Установка

```bash
pip install -r requirements.txt
```

## Использование

### Базовое использование
```bash
python3 listen-minechat.py
```

### С настройками
```bash
python3 listen-minechat.py --host ... --port ... --history ~/minechat.history
```

### Помощь
```bash
python3 listen-minechat.py --help
```

## Переменные окружения

Можно настроить поведение через переменные окружения:

```bash
export MINECHAT_HOST=...
export MINECHAT_PORT=...
export MINECHAT_HISTORY=...

python3 listen-minechat.py
```

## Аргументы командной строки

| Аргумент | Переменная окружения | По умолчанию | Описание |
|----------|---------------------|--------------|----------|
| `--host` | `MINECHAT_HOST` | `...` | Хост сервера |
| `--port` | `MINECHAT_PORT` | `...` | Порт сервера |
| `--history` | `MINECHAT_HISTORY` | `...` | Путь к файлу истории |

## Формат лога

Сообщения сохраняются в формате:
```
[дд.мм.гг чч:мм] Сообщение
```

Пример:
```
[19.02.19 20:34] Установлено соединение с ...
[19.02.19 20:34] Eva: ...
[19.02.19 20:35] Vlad: ...
```

## Завершение работы

Нажмите `Ctrl+C` для корректного завершения работы программы.
